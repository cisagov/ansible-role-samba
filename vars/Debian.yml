---
# The packages necessary for a Samba client
client_packages:
  - cifs-utils
  - samba-common

# The additional packages necessary for a Samba server
server_packages:
  - samba

# The name of the Samba service
service_name: smbd
