---
# The packages necessary for a Samba client
client_packages:
  - cifs-utils

# The packages necessary for a Samba server
server_packages:
  - samba

# The name of the Samba service
service_name: smb
